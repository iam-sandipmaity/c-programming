#include<stdio.h>
#include<string.h>

int main() {
    char *ptr = "Sndip";
    int a = strlen(ptr);      //strlen() is the function to count any string lenght 
    printf("The Length Of The String Is : %d",a);
    return 0;
}